spillingTeaAbout characters

pluh main() : int {
    /* create a variable that stores a char
    and assign it a value */
    cookUp x : char = 'a'

    fr? (x == 'a') {
        yap("x is a")
    } ong? (x == 'b') {
        yap("x is b")
    } ong? (x == 'c') {
        yap("x is c")
    } justLikeThat? {
        yap("x is not a, b, or c")
    }
    yeet 0
}